# M9 — PR CI gate

**Verified local (25/09/2026):** 29 tests pass (1 MySQL integration opt-in skipped trong suite mặc định), build pass, MySQL container smoke pass, image scan 0 HIGH/CRITICAL, 14 policy tests pass. Bốn source scanner thật đạt `PASS WITH WARNINGS`: 0 secret/Critical/High, 4 Medium + 4 Low IaC.

**Verified on GitHub (25/09/2026):** [PR #1](https://github.com/qkhanhbe/HanoiTrip/pull/1) đi qua app-check, container-check, Terraform checks/plan-status, Gitleaks, Trivy dependency/SBOM/misconfig, Semgrep và central `source-security-gate`. Run đầu Semgrep đỏ do workflow expression bị parser coi là Bash; fix được push trên cùng PR và run sau xanh. Ruleset `protect-main` active, không có bypass, bắt buộc PR + squash + các gate chính.

**Pending GitHub evidence:** PR secret giả đỏ và bị chặn merge; PR image/high finding đỏ; warning-only PR pass; Terraform plan artifact thật sau khi có Azure OIDC. `terraform-plan` hiện xanh với warning `pending`, không được tính là plan Azure.
