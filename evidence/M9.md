# M9 — PR CI gate

**Verified local (25/09/2026):** 29 tests pass (1 MySQL integration opt-in skipped trong suite mặc định), build pass, MySQL container smoke pass, image scan 0 HIGH/CRITICAL, 14 policy tests pass. Bốn source scanner thật đạt `PASS WITH WARNINGS`: 0 secret/Critical/High, 4 Medium + 4 Low IaC.

**Implemented:** PR-only app/Terraform/container CI, source scan + SBOM/SARIF + central fail-closed gate, optional OIDC Terraform plan artifact. PR CI không có deploy identity; plan identity tách riêng và chỉ bật khi cấu hình.

**Pending GitHub evidence:** Actions run xanh, ruleset chặn direct/failed merge; PR secret giả đỏ; PR image/high finding đỏ; warning-only PR pass; Terraform plan artifact sau khi có Azure OIDC.
