# M6 — IP allowlist

**Implemented:** production/staging access restriction `Deny` mặc định và allow các CIDR được nhập. CD chỉ mở runner IP tạm rồi cleanup trong `always()`.

**Pending Azure evidence:** curl từ IP được phép trả 200 và từ mạng khác trả 403; kiểm tra không còn rule runner sau release.
