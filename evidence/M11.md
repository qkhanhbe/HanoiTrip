# M11 — Monitoring

**Verified local:** JSON logs chứa requestId/buildSha/statusCode/duration; `/boom` trả correlated 500 và `/load` bị giới hạn/rate-limit khi diagnostics bật.

**Implemented in Terraform:** App Service/MySQL diagnostics về Log Analytics, action group email, alert 5xx/CPU/MySQL connections và autoscale. `runbook.md` xử lý 5xx.

**Pending Azure evidence:** dashboard không dùng App Insights, KQL trên log thật, ba alert active, email và ít nhất một alert được trigger thật.
