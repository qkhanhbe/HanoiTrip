# M10 — CD từ main

**Implemented:** chỉ trigger từ `main`/manual; OIDC, build một image SHA, Trivy, push ACR, migration, staging warm-up, production observer, swap/rollback, raw artifact và cleanup IP.

**Safety state:** deploy job chỉ chạy khi repository variable `AZURE_CD_ENABLED=true`; mặc định hiện tại là pending, không giả deploy.

**Verified trigger only (25/09/2026):** sau khi merge PR #1, [CD run 36104618052](https://github.com/qkhanhbe/HanoiTrip/actions/runs/36104618052) được trigger từ `main`; status pass và deploy skipped đúng safety state.

**Pending Azure evidence:** một GitHub run end-to-end và SHA image/slot/output tương ứng. Run trigger-only ở trên không phải bằng chứng M10 hoàn thành.
