# M1 — Docker image, ACR và Managed Identity

**Implemented:** multi-stage non-root image; Terraform tắt ACR admin, bật system-assigned MI cho hai slot và gán `AcrPull` đúng ACR scope. Local image build/smoke và Trivy HIGH/CRITICAL scan đạt ngày 25/09/2026.

**Pending Azure evidence:** ACR config/role output, image SHA trong registry, App Service đang chạy đúng SHA mà không dùng registry password.
